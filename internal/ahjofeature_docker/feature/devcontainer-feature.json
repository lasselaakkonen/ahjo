{
  "id": "docker",
  "version": "1.0.0",
  "name": "Docker (ahjo built-in)",
  "description": "Installs Docker Engine + compose plugin into an ahjo container. Requires security.nesting, which this Feature requests automatically via customizations.ahjo.nesting — no mounts, no privileged, and no customizations.ahjo.nested_incus required.",
  "options": {
    "version":     { "default": "latest" },
    "channel":     { "default": "stable" },
    "daemon_args": { "default": "" }
  },
  "containerEnv": {
    "DOCKER_BUILDKIT": "1"
  },
  "customizations": {
    "ahjo": {
      "nesting": true
    }
  }
}
