/ahjo
/dist/

# Build artifacts: embedded ahjo-mirror daemon binaries. Regenerated by
# `make generate-mirror` (or any `make build`/CI step); kept out of git so
# Go's per-build BuildID doesn't churn the diff. The devcontainer
# postCreateCommand and CI both run generate-mirror before vet/test.
/internal/ahjoruntime/feature/ahjo-mirror.linux-amd64
/internal/ahjoruntime/feature/ahjo-mirror.linux-arm64

# Local Claude Code harness state (per-project agent scratch).
.claude/
